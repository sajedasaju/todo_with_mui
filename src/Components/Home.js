import { createTheme, Paper, Table, TableBody, TableCell, TableContainer, TableHead, TableRow } from '@mui/material';
import React from 'react';
import AllData from './AllData';
import Lifecycle from './Lifecycle';


const Home = () => {

    return  (
        <div>
          <AllData />
          
        </div>
      );
}

export default Home;
